python manage.py migrate    hacemos la migracion # tablas por defecto
Realizar la migración, para crear las tablas por defecto del proyecto en la base de datos

en settings.py del proyecto=rutas globales
INSTALLED_APPS = [
    'django.contrib.auth',   # hace la autenticacion
    'contacto',   // Agregamos el nombre la aplicacion para poder agregarle plantillas
    'users',
]

python manage.py startapp users


creamos el superusuario: python manage.py createsuperuser
python manage.py changepassword admin  ·definimos un nuevo password para admin
podemos ingresar en modo adminsitrador: localhost:8000/admin
creamos los user

hacemos la migracion: python manege.py makemigrations
hacemos la migracion: python manege.py migrate
